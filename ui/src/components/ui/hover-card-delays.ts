/**
 * How long a hover card waits before it opens, and before it shuts.
 *
 * Held apart from the control so both halves of a pair agree: the trigger
 * takes the open delay and the group takes the close delay.
 */
export const HOVER_CARD_OPEN_DELAY = 120
export const HOVER_CARD_CLOSE_DELAY = 200
