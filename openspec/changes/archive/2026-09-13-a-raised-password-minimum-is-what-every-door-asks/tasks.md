# Tasks

## 1. The bound reaches the doors

- [x] 1.1 Read the install's minimum where a password is written, per request rather than at boot
- [x] 1.2 Cover the doors the library serves itself, which reach no controller
- [x] 1.3 Answer a refusal for length as itself, not as a wrong current password

## 2. What it must not do

- [x] 2.1 Leave sign-in alone, so a raise locks nobody out of an account they already hold
- [x] 2.2 Keep the number away from a caller who has not been authenticated

## 3. Left open

- [ ] 3.1 Tell an analyst the minimum before they type, rather than on refusal — the policy route is `@AdminOnly`, so no screen an analyst reaches can read it
