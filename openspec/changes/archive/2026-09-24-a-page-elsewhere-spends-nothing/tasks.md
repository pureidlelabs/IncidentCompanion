# Tasks

## 1. A page elsewhere

- [x] 1.1 The edge refuses a request another site sent, by its origin or its fetch metadata, on every route before any limit
- [x] 1.2 The shipped stack refuses a cross-site flood on a route other than sign-in and still serves the analyst and a link

## 2. The edge started last

- [x] 2.1 The door looks the edge up before attributing a peer it has not checked lately
- [x] 2.2 The shipped stack, its edge started after the application, records each analyst at their own address
