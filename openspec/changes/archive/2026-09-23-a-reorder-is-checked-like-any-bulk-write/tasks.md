# Tasks

## 1. The route

- [x] 1.1 Take each row's version, lock the set and refuse what moved, and verify with `server/test/two-reorders-never-mix.test.ts` and `server/src/collections/reorder.test.ts`

## 2. The screen

- [x] 2.1 Send the versions the screen holds and put the order back on a refusal, and verify with `ui/src/api/useEntryReorder.test.tsx`
