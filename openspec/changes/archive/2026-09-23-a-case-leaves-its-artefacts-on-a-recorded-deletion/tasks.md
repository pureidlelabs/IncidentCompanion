# Tasks

## 1. A case's artefacts leave on a record of its deletion

- [x] 1.1 At start, remove a case's directory only when the install recorded deleting the case
- [x] 1.2 Remove the artefacts of the demonstrations the rebuild deletes, from the server's evidence directory

## 2. Found beside it

- [x] 2.1 Remove at start what an earlier layout left outside any case directory
- [x] 2.2 Count bytes stored again as stored now, for the grace
- [x] 2.3 One definition of which figures a document places, for render, the start and the archive
- [x] 2.4 Ask the cases what they name once at start, for the removal and the count

## 3. Left open

- [ ] 3.1 The start does not say that it found artefacts of a case no database holds and none recorded deleting; an operator learns it only by looking.
