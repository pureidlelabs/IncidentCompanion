# Design

**A guard reading past the caller answers only inside the caller's reach.** A guard that reads another row as the tables' owner to decide about a write reads it only for a row the boundary admits, so a row the boundary refuses gets the boundary's own refusal and nothing else. A named row outside the written row's own case is refused as a missing reference is, before the reference check reaches it, so it is neither reported nor locked.
