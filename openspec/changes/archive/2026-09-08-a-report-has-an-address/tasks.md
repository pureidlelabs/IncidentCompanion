# Tasks

## 1. The address names the open report

- [x] 1.1 Read the open report from the address and hand it to the section
- [x] 1.2 Write the open report into the address when one is opened, and take it out again on the index
- [x] 1.3 Replace rather than push, so Back leaves the section rather than walking its reports
- [x] 1.4 Compose the write from the address itself, so a command already carried out is not written back

## 2. The section follows the address

- [x] 2.1 Let the caller that owns the address own the answer, so a change after the first draw moves the screen
- [x] 2.2 Keep the section drawable with no address at all, for the gallery and for its own tests

## 3. What the change made false

- [x] 3.1 Stop the prodding sweep reading a search parameter as a navigation, and keep it reading a fragment as one
- [x] 3.2 Correct the two browser specs that record a report as having no address of its own
