/**
 * What this provider is called, wherever a row has to name where it came from.
 *
 * A timeline entry carries it in `sourceTool` and an entity row in `source`.
 */
export const PLATFORM = 'Microsoft Sentinel'
