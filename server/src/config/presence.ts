/**
 * How long a connection that ended without notice stays on a case's roster,
 * at most, in seconds.
 *
 * The store expires a member on it and the install's own description states
 * it, so what an analyst is told and what the roster does are one number.
 */
export const PRESENCE_BOUND_SECONDS = 30
