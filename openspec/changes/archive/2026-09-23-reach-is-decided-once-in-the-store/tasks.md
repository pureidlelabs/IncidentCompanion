# Tasks

## 1. The store decides reach

- [x] 1.1 One decision for the level over a customer and over a case, created by every schema application before the policies
- [x] 1.2 An account the install does not hold reaches nothing, the default customer included
- [x] 1.3 Policies on every case table, `cases` and `case_visits`, per command and by level
- [x] 1.4 The quantifier seeds every table holding case data and sweeps it as a principal who reaches nothing, as an id the install does not hold, and unscoped, with a reaching principal as the control

## 2. Who is asking is set once

- [x] 2.1 A request names its principal on arrival; a socket names it per frame
- [x] 2.2 A scope with nobody named is refused before it reaches the store
- [x] 2.3 A live document writes itself as the analyst whose edit it last took
- [x] 2.4 The seed one-shot acts as the seeding role throughout, and the unattributed-case backfill moves there

## 3. Every door asks the one decision

- [x] 3.1 The route guard and the case socket ask the same question, and the socket's own copy is deleted
- [x] 3.2 The case list and the recent-cases list are filtered by the store, and the list predicate is deleted
- [x] 3.3 A refusal does the same work whether or not the case exists, and is recorded once the answer has gone
- [x] 3.4 The route and the socket answer alike at every level
- [x] 3.5 The administrator's view of a level answers the store's level

## 4. What sees past a caller's reach

- [x] 4.1 The merge's reference check, its move and the count behind a customer are the store's acts, and refuse a principal who is not an administrator
- [x] 4.2 The case tally and the artefact census answer counts and digests only
- [x] 4.3 A merge refusal names the cases by identifier and reference, never by title

## 5. The move

- [x] 5.1 A case carrying a reference moves only to a customer the mover reaches, with one fixed answer elsewhere
- [x] 5.2 A mover reaching the destination is told which case holds the reference
- [x] 5.3 The move itself is the store's act, asked for the mover's write on the case where it is

## 6. Left open

- [ ] 6.1 A session whose account was deleted outside the authentication library is still served by the library from the ephemeral store; the store now refuses it everything, and ending the session itself belongs to the authentication work.
- [ ] 6.2 A refused reach by such a session writes no audit line, because the line names an account that is gone. -> #75
