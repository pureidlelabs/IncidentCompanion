# Tasks

## 1. Preparation

- [x] 1.1 Take every table at once without waiting, retrying while any is in use
- [x] 1.2 Refuse a rename-shaped change, naming both sides
- [x] 1.3 Run when named through a linked path

## 2. Copies

- [x] 2.1 Record each part's digest when a copy is taken, and compare it when one is checked
- [x] 2.2 Keep a copy written by default out of git and out of the build context
- [x] 2.3 Prove a restore signs out the install it is restored into

## 3. Evidence

- [x] 3.1 A write beside preparation, a renamed column and index, and a linked path, in the schema-step test
- [x] 3.2 A flipped byte and a restore into the same install, in the lifecycle tier
- [x] 3.3 Each of seeding's two guards on an install of its own
- [x] 3.4 The certifying sweep runs the lifecycle tier
