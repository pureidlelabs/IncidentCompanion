# Tasks

## 1. Bytes leave with what named them

- [x] 1.1 Remove a deleted record's bytes when nothing else in its case names them, for a single delete and a selection
- [x] 1.2 Remove what a record named before its file was replaced, and what a refused attachment brought
- [x] 1.3 Remove, once an archive's case commits, what it carried and none of its records name
- [x] 1.4 Put acts on one case's artefacts in order, from an attachment's bytes landing to its record being written, across every module's store

## 2. The start removes nothing

- [x] 2.1 Remove the start-up sweep, its grace period and its reading of the deletion record
- [x] 2.2 Count stored artefacts nothing names, say the count at start and serve it in the install's description
