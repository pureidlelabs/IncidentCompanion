# Tasks

## 1. One rule, asked three ways

- [x] 1.1 Publish the question as helpers a caller asks, rather than as a walk each caller writes
- [x] 1.2 Index a stored row under every naming it answers to, not under one key
- [x] 1.3 Point the spreadsheet door, the incident door and the CSV preview at them
- [x] 1.4 Delete the second index builder and the second lookup rather than correcting them

## 2. What it must not do

- [x] 2.1 Match below a floor the ladder declares
- [x] 2.2 Match a binary named without a hash against one stored with a different hash
- [x] 2.3 Pick differently between two stored rows of one naming on different runs

## 3. Left open

- [ ] 3.1 What a replace writes once a row is matched on a rung weaker than the file stated
- [ ] 3.2 `enrich` widens a candidate's fields after its match is resolved and does not re-match
- [ ] 3.3 `MappedEntity.identities` has no production consumer now that matching recomputes from the fields; its docstring still says matching walks it
