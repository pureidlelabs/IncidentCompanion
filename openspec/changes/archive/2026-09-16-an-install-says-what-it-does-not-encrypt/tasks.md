# Tasks

## 1. The install says it

- [x] 1.1 State, on the route that already describes where the state lives, that the application writes all of it unencrypted
- [x] 1.2 Say what confidentiality at rest rests on instead, so the absence reads as a division of responsibility rather than as a defect
- [x] 1.3 Name evidence, which is the thing an operator is likeliest to assume is treated differently

## 2. The obligation becomes demonstrable

- [x] 2.1 Give the requirement the scenario it had no way to be shown by
- [x] 2.2 Assert the three properties rather than the sentence, so rewording is free and dropping a half is not

## 3. Left open

- [ ] 3.1 No screen draws the install reference -- an operator reads it through the API document. That is how the evidence note is read too, so this changes nothing about reach, but neither note is in front of anybody who has not gone looking.
