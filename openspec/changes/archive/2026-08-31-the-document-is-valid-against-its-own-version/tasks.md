# Tasks

## 1. Make the declaration true

- [x] 1.1 Declare the specification the schemas are actually written in, rather than the builder's default
- [x] 1.2 Confirm nothing asserted the old declaration -- the eight occurrences are fixtures feeding `tidy`, where the field is required and its value arbitrary

## 2. Hold it

- [ ] 2.1 Confirm the description lints clean against its declared version, with the stack up
- [ ] 2.2 Confirm the rendered reference still draws, which no test can see

## 3. Land it

- [ ] 3.1 Sync the delta into `specs/the-api`, archive the change
