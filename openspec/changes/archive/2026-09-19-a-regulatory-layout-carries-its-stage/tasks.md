# Tasks

## 1. A layout says which step it files

- [x] 1.1 Let a layout declare its step beside the regime it belongs to, so one place states both
- [x] 1.2 Declare it on each of the four regulatory layouts the application ships
- [x] 1.3 Leave the stage vocabulary alone, which nothing has to match once no label is read for a step

## 2. The value reaches the analyst

- [x] 2.1 Carry it every hop the regime flag already takes, including the seeded payload and the demo capture
- [x] 2.2 Build the seeded payload once rather than once per branch of the upsert, so an addition cannot reach one branch and not the other
- [x] 2.3 Make the field always present on the wire, empty for a layout that files no step

## 3. The obligation is demonstrable

- [x] 3.1 Assert that every layout under a regime declares a step the vocabulary holds
- [x] 3.2 Assert that a layout under no regime claims none
- [x] 3.3 Assert that the route answers with the declared step, and with nothing for an ordinary layout
