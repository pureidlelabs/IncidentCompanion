# Tasks

## 1. The runs

- [x] 1.1 A booted-app test presenting distinct client addresses, red on the account-wide lock: the holder refused after another machine guessed, a repeated password locking, locks that never grow
- [x] 1.2 Count each failure in the familiar or the unfamiliar run, chosen by the address the authentication library resolves; lock only that run's addresses
- [x] 1.3 Grow each consecutive lock up to the install's maximum; reset the count when a lock falls
- [x] 1.4 Count a remembered wrong password once, keeping only a keyed hash
- [x] 1.5 Clear both runs on an administrator's release

## 2. On the shipped topology

- [x] 2.1 The shipped Compose reached from analyst containers: one machine guessing leaves another analyst signing in from their own

## 3. The record

- [x] 3.1 Requirement, design record, matrix and ledger rows; sync and archive
