# Tasks

## 1. The requirement the timeline had no half of

- [x] 1.1 State that an entry an earlier import wrote is recognised and not written again
- [x] 1.2 State that only an import's own work is recognised, never an entry an analyst wrote
- [x] 1.3 State that a recognised row is reported as already in the case rather than as added
- [x] 1.4 Narrow the store's rule to what is supplied, so the import's question is the import's

## 2. The import judges its timeline where it judges its entities

- [x] 2.1 Read the entries the case holds, and recognise an arriving alert against them
- [x] 2.2 Offer a recognised entry unticked, and say what it matched
- [x] 2.3 Skip it at the write whatever was ticked, and count it as already there

## 3. The review draws it

- [x] 3.1 Read the verdict off what the candidate matched, as the entity half does

## 4. Left open

- [ ] 4.1 Carrying the platform's own identifier for an alert on the row it wrote. An entry an analyst has re-described is not recognised without it, and the import writes a second copy. A column and a migration.
