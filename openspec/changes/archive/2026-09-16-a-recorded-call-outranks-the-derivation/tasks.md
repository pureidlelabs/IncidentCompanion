# Tasks

## 1. The requirement that was missing

- [x] 1.1 State that a recorded determination is the one the assessment carries
- [x] 1.2 State that the assessment says which of the two it is
- [x] 1.3 State that scope is not settled this way

## 2. The verdict reads what the analyst recorded

- [x] 2.1 Consult the recorded call before deriving, in the one function both callers reach
- [x] 2.2 Name it as recorded, with the article it answers
- [x] 2.3 Keep scope a gate part, so an unclassified entity stays undecidable

## 3. The mirror nothing called

- [x] 3.1 Remove `unanswered`, which has never had a caller

## 4. Left open

- [ ] 4.1 Surfacing a recorded call that disagrees with what the criteria derive. Nothing computes the derivation once a call is recorded, so there is no second verdict to compare against.
- [ ] 4.2 `regimes` is the same shape on a different column, and its fix is a different one. → #132
