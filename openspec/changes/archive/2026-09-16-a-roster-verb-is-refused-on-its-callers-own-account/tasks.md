# Tasks

- [x] Refuse ending the sessions of the account the request is made with, at the route, the shape `disable` already used
- [x] Refuse a role *change* on that account, after the last-administrator rule so somebody who is both hears the thing they can act on
- [x] Leave the row's verbs off the caller's own row on both panes that draw the roster, from the served `you` rather than a name the client matches
- [x] Demonstrate the scenarios: each verb on the caller's own account, the no-op role, and a control that both still work on somebody else
- [x] Fold this delta into `openspec/specs/accounts-and-access/spec.md` and archive the change, before the merge
