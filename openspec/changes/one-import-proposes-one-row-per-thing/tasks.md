# Tasks

## 1. The plan is an index of its own rows

- [x] 1.1 Keep an index of what the plan already proposes, keyed the way the case's rows are
- [x] 1.2 Ask both indexes with the same ladder walk, so a weaker naming matches a stronger one
- [x] 1.3 Point a later incident's entity reference at the candidate the first proposed

## 2. What it must not do

- [x] 2.1 Leave two incidents naming different things as two rows
- [x] 2.2 Leave events unmatched against each other, here as against the case

## 3. Left open

- [ ] 3.1 Say in the preview which other incidents named a row somebody else proposed — the candidate carries one incident key, and widening it is a screen change as much as a service one
