# Tasks

## 1. One table for the provider's ladder

- [x] 1.1 Read the provider's severity word in one place, so the alert mapper and the case seed cannot disagree
- [x] 1.2 Answer nothing rather than a level for a word the vocabulary cannot say, and let each caller decide what that means

## 2. The case the import opens

- [x] 2.1 Carry the provider's word across the boundary unmapped, beside the title it already carries
- [x] 2.2 Derive the case's severity from the incidents in the request
- [x] 2.3 Take the worst where one case is opened from several
- [x] 2.4 Stop offering a severity on the create door, which no caller could fill

## 3. What the change made visible

- [x] 3.1 Send the provider's word from the wizard, which composed no level and so sent nothing at all
- [x] 3.2 Assert the mapping rather than the round trip, where a fixture was sending a spelling no provider uses
- [x] 3.3 Annotate the payload the wizard builds rather than casting it, so a field dropped there is a compile error
